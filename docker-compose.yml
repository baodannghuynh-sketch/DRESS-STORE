version: '3.8'

services:
  app:
    image: php:8.2-cli
    container_name: laravel_app
    working_dir: /app
    volumes:
      - .:/app
    command: php artisan serve --host=0.0.0.0 --port=8000
    ports:
      - "8000:8000"
